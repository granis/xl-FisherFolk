on:
  workflow_dispatch:
  push:
    branches:
      - "master"

jobs:
  build_latest:
    name: Build latest
    uses: ./.github/workflows/build.yml
    with:
      autotag: "latest"
      prerelease: true
      title: "Development build"
